<UserControl x:Class="Fasetto.Word.HierarchyManagementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             
             d:DesignHeight="300" d:DesignWidth="300">
    <!--<Grid DataContext="{x:Static local:ChatListDesignModel.Instance}" Background="{StaticResource ForegroundLightBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:ChatListItemControl />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>-->
    <DockPanel>
        <!-- SEARCH AREA -->
        <Border 
      DockPanel.Dock="Bottom" 
      BorderBrush="Gray" 
      BorderThickness="1,0,1,1"
      >
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#33000000" Offset="0" />
                    <GradientStop Color="#11000000" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>

            <StackPanel 
        HorizontalAlignment="Center" 
        Margin="4" 
        Orientation="Horizontal"
        >
                <TextBlock Text="Search for:" />
                <TextBox 
          x:Name="searchTextBox"
          KeyDown="SearchTextBox_KeyDown" 
          Margin="6,0"
          Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
          Width="150"
          />
                <Button 
          Command="{Binding SearchCommand}" 
          Content="_Find" 
          Padding="8,0" 
          />
            </StackPanel>
        </Border>

        <!-- FAMILY TREE -->
        <TreeView 
          ItemsSource="{Binding FirstGeneration}" 
          Name="tvParameters"
                                AllowDrop="True"  MouseDown="TreeView_MouseDown" DragOver="TreeView_DragOver" Drop="TreeView_Drop" MouseMove="TreeView_MouseMove">
            <TreeView.ItemContainerStyle>
                <!-- 
        This Style binds a TreeViewItem to a CategoryViewModel. 
        -->
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                    <Setter Property="AllowDrop"  Value="True"/>
                    <Setter Property="AllowDrop"  Value="True"/>
                    <Setter Property="FontWeight" Value="Normal" />
                    <EventSetter Event="DragOver"  Handler="TreeView_DragOver"/>
                    <EventSetter Event="Drop" Handler="TreeView_Drop"/>
                    <EventSetter Event= "MouseDown" Handler="TreeView_MouseDown"/>
                    <!--<EventSetter Event="MouseMove" Handler="TreeView_MouseMove"/>-->
                    <!--<EventSetter Event="MouseDown" Handler="TreeView_MouseDown"/>-->
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="FontWeight" Value="Bold" />
                        </Trigger>
                        <!--<Trigger Property="AllowDrop" Value="True">
                    <Setter Property="FontWeight" Value="Bold" />-->
                        <!--
                </Trigger>-->
                    </Style.Triggers>
                </Style>
            </TreeView.ItemContainerStyle>

            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding ShortName}" Background="AliceBlue" Foreground ="Black"/>
                        <TextBlock Text=" "/>

                        <TextBlock Text="{Binding  KCategoryId}" />
                    </StackPanel>
                    <!--<TextBlock Text="{Binding KCategoryId}" />-->
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

    </DockPanel>
</UserControl>

